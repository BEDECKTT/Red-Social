@{
    ViewData["Title"] = "Reacciones";
}

<h2>Reacciones</h2>

@(Html.DevExtreme().DataGrid<AppDevextreme.Models.Reaccion>()
    .DataSource(ds => ds.WebApi()
        .Controller("SampleData")
        .LoadAction("Get")
        .InsertAction("Post")
        .UpdateAction("Put")
        .DeleteAction("Delete")
        .Key("Id")
    )
    .RemoteOperations(true)
    .Columns(columns => {

        columns.AddFor(m => m.Id);

        columns.AddFor(m => m.Tipo);

        columns.AddFor(m => m.FechaReaccion);
    })
    .Editing(e => e
        .AllowAdding(true)
        .AllowUpdating(true)
        .AllowDeleting(true)
    )
)
